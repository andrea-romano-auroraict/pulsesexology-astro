<section class="hero section">
  <div class="hero__shape hero__shape--clay" aria-hidden="true"></div>
  <div class="hero__shape hero__shape--sage" aria-hidden="true"></div>

  <div class="container hero__inner panel">
    <p class="kicker">Counselling & Sexology Support</p>
    <h1 class="hero__title">A gentle place to work through intimacy, connection, and change</h1>
    <p class="hero__lead">
      Whether you are coming as a couple or on your own, this is a grounded space to explore relationship challenges,
      menopause and midlife changes, and your sexual wellbeing with care and respect.
    </p>
    <div class="hero__actions">
      <a class="btn btn--primary" href="#pathways">Explore support pathways</a>
      <a class="btn btn--secondary" href="#about">Meet Simone</a>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: clamp(4.7rem, 10vw, 7.6rem);
    isolation: isolate;
  }

  .hero__shape {
    position: absolute;
    z-index: -1;
    border-radius: 45% 55% 50% 50% / 58% 48% 52% 42%;
    filter: blur(1px);
  }

  .hero__shape--clay {
    width: clamp(220px, 28vw, 360px);
    height: clamp(170px, 22vw, 270px);
    top: 3.2rem;
    right: max(1rem, calc((100vw - 1100px) / 2));
    background: radial-gradient(circle at 35% 30%, rgba(191, 152, 116, 0.34), rgba(191, 152, 116, 0.05));
  }

  .hero__shape--sage {
    width: clamp(190px, 24vw, 290px);
    height: clamp(130px, 18vw, 220px);
    bottom: 0.5rem;
    left: max(0.8rem, calc((100vw - 1100px) / 2));
    background: radial-gradient(circle at 40% 40%, rgba(134, 162, 131, 0.32), rgba(134, 162, 131, 0.04));
  }

  .hero__inner {
    max-width: 860px;
    padding: clamp(1.8rem, 4vw, 2.8rem);
    position: relative;
  }

  .hero__title {
    margin: 0;
    font-family: "Georgia", "Times New Roman", serif;
    font-size: clamp(2.2rem, 6.2vw, 4rem);
    line-height: 1.1;
    font-weight: 500;
    max-width: 15ch;
  }

  .hero__lead {
    margin: 1.35rem 0 0;
    font-size: 1.08rem;
    color: var(--color-text-muted);
    max-width: 65ch;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 2rem;
  }

  @media (max-width: 900px) {
    .hero__shape {
      display: none;
    }

    .hero__inner {
      max-width: 100%;
    }
  }
</style>
