<header class="site-header">
  <div class="site-header__glow" aria-hidden="true"></div>
  <div class="container site-header__inner">
    <a class="site-wordmark" href="/" aria-label="Pulse Sexology home">
      <span class="site-wordmark__pulse">Pulse</span>
      <span class="site-wordmark__sexology">Sexology</span>
    </a>

    <nav aria-label="Primary">
      <ul class="site-nav">
        <li><a href="/">Home</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#pathways">Work With Me</a></li>
        <li><a href="#book">Relationships Rewire</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>

    <a class="btn btn--primary site-header__cta" href="#contact">Book a Session</a>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 20;
    border-bottom: 1px solid color-mix(in srgb, var(--color-border) 84%, #fff 16%);
    background: color-mix(in srgb, var(--color-bg) 84%, #fff 16%);
    backdrop-filter: blur(16px);
    isolation: isolate;
  }

  .site-header__glow {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(42rem 9rem at 10% 0%, rgba(168, 138, 112, 0.15), transparent 70%),
      radial-gradient(36rem 7rem at 90% 8%, rgba(117, 141, 114, 0.11), transparent 75%);
    z-index: -1;
  }

  .site-header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.2rem;
    min-height: 88px;
  }

  .site-wordmark {
    display: grid;
    line-height: 1;
    letter-spacing: 0.02em;
    min-width: 170px;
  }

  .site-wordmark__pulse {
    font-family: "Georgia", "Times New Roman", serif;
    font-size: 1.62rem;
    font-style: italic;
    color: #624936;
  }

  .site-wordmark__sexology {
    margin-top: 0.2rem;
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.34em;
    text-transform: uppercase;
    color: #7d6a57;
  }

  .site-nav {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    gap: 1.35rem;
    color: var(--color-text-muted);
    font-size: 0.95rem;
  }

  .site-nav a {
    padding: 0.25rem 0.1rem;
    transition: color 160ms ease;
  }

  .site-nav a:hover {
    color: var(--color-text);
  }

  .site-header__cta {
    white-space: nowrap;
    font-size: 0.95rem;
    font-weight: 700;
    letter-spacing: 0.01em;
    padding: 0.9rem 1.4rem;
    border-radius: 16px;
    box-shadow: 0 9px 22px rgba(79, 56, 39, 0.2);
    border: 1px solid rgba(255, 255, 255, 0.35);
  }

  @media (max-width: 980px) {
    .site-header {
      position: static;
    }

    .site-header__inner {
      flex-wrap: wrap;
      justify-content: center;
      padding: 1rem 0 1.2rem;
      gap: 0.8rem 1rem;
      min-height: 0;
    }

    .site-wordmark {
      justify-items: center;
    }

    .site-nav {
      flex-wrap: wrap;
      justify-content: center;
      row-gap: 0.5rem;
    }
  }
</style>
