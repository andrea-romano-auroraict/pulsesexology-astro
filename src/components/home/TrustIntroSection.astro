<section class="section section--muted trust-wrap">
  <div class="container trust-intro panel">
    <p>
      Intimacy can shift over time. Relationships evolve, bodies change, and sometimes words are hard to find. You do
      not need to have it all figured out before reaching out—support can begin right where you are.
    </p>
  </div>
</section>

<style>
  .trust-wrap::after {
    content: "";
    position: absolute;
    width: clamp(180px, 22vw, 260px);
    height: clamp(120px, 16vw, 180px);
    right: max(0.8rem, calc((100vw - 1080px) / 2));
    bottom: 1.1rem;
    border-radius: 50%;
    background: radial-gradient(circle at center, rgba(165, 183, 154, 0.22), transparent 70%);
    pointer-events: none;
  }

  .trust-intro {
    max-width: 900px;
    padding: clamp(1.5rem, 4vw, 2.2rem);
  }

  .trust-intro p {
    margin: 0;
    font-size: clamp(1.12rem, 2.4vw, 1.45rem);
    font-family: "Georgia", "Times New Roman", serif;
    line-height: 1.55;
    color: #4b433b;
  }
</style>
