<header class="site-header">
  <div class="site-header__glow" aria-hidden="true"></div>
  <div class="container site-header__inner">
    <a class="site-wordmark" href="/" aria-label="Pulse Sexology home">
      <span class="site-wordmark__pulse">Pulse</span>
      <span class="site-wordmark__sexology">Sexology</span>
    </a>

    <nav aria-label="Primary">
      <ul class="site-nav">
        <li><a href="/">Home</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#pathways">Work With Me</a></li>
        <li><a href="#book">Relationships Rewire</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>

    <a class="btn btn--primary site-header__cta" href="#contact">Book a Session</a>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 20;
    border-bottom: 1px solid color-mix(in srgb, var(--color-border) 84%, #fff 16%);
    background: color-mix(in srgb, var(--color-bg) 84%, #fff 16%);
    backdrop-filter: blur(16px);
    isolation: isolate;
  }

  .site-header__glow {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(42rem 9rem at 10% 0%, rgba(168, 138, 112, 0.15), transparent 70%),
      radial-gradient(36rem 7rem at 90% 8%, rgba(117, 141, 114, 0.11), transparent 75%);
    z-index: -1;
  }

  .site-header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.2rem;
    min-height: 96px;
  }

  .site-wordmark {
    display: grid;
    line-height: 1;
    letter-spacing: 0.024em;
    min-width: 214px;
    padding: 0.2rem 0;
    position: relative;
  }

  .site-wordmark__pulse {
    font-family: "Georgia", "Times New Roman", serif;
    font-size: clamp(1.88rem, 1.55rem + 1vw, 2.3rem);
    font-style: italic;
    font-weight: 500;
    color: #5f4533;
    text-shadow: 0 1px 0 rgba(255, 255, 255, 0.45);
  }

  .site-wordmark__sexology {
    margin-top: 0.25rem;
    font-size: 0.76rem;
    font-weight: 600;
    letter-spacing: 0.36em;
    text-transform: uppercase;
    color: #715d49;
  }

  .site-wordmark::after {
    content: "";
    width: 88%;
    height: 1px;
    margin-top: 0.45rem;
    background: linear-gradient(to right, rgba(111, 87, 69, 0.6), rgba(111, 87, 69, 0.1));
    border-radius: 999px;
  }

  .site-nav {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    gap: 0.52rem;
    color: var(--color-text);
    font-size: 0.9rem;
  }

  .site-nav a {
    padding: 0.48rem 0.92rem;
    border: 1px solid color-mix(in srgb, var(--color-border) 80%, #fff 20%);
    border-radius: 999px;
    background: color-mix(in srgb, var(--color-surface) 78%, #fff 22%);
    box-shadow: 0 3px 10px rgba(73, 49, 31, 0.06), inset 0 1px 0 rgba(255, 255, 255, 0.65);
    transition: color 160ms ease, border-color 160ms ease, background-color 160ms ease,
      transform 160ms ease;
  }

  .site-nav a:hover {
    color: var(--color-text);
    border-color: color-mix(in srgb, var(--color-accent) 28%, var(--color-border) 72%);
    background: color-mix(in srgb, var(--color-surface) 62%, #fff 38%);
    transform: translateY(-1px);
  }

  .site-header__cta {
    white-space: nowrap;
    font-size: 0.95rem;
    font-weight: 700;
    letter-spacing: 0.01em;
    padding: 0.9rem 1.4rem;
    border-radius: 16px;
    box-shadow: 0 9px 22px rgba(79, 56, 39, 0.2);
    border: 1px solid rgba(255, 255, 255, 0.35);
  }

  @media (max-width: 980px) {
    .site-header {
      position: static;
    }

    .site-header__inner {
      flex-wrap: wrap;
      justify-content: center;
      padding: 1rem 0 1.2rem;
      gap: 0.8rem 1rem;
      min-height: 0;
    }

    .site-wordmark {
      justify-items: center;
    }

    .site-nav {
      flex-wrap: wrap;
      justify-content: center;
      row-gap: 0.5rem;
    }
  }
</style>
