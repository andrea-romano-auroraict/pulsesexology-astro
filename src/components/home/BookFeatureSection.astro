<section id="book" class="section">
  <div class="container book-feature">
    <article class="book-feature__card panel">
      <figure class="book-feature__image-frame">
        <img
          src="/images/relationships-rewire.webp"
          alt="Relationships Rewire feature image in warm neutral tones."
          class="book-feature__image"
          loading="lazy"
          width="460"
          height="620"
        />
      </figure>
      <div>
        <p class="kicker">Featured Resource</p>
        <h2 class="section__heading">Relationships Rewire</h2>
        <p class="section__lead">
          Relationships Rewire offers practical reflections and compassionate prompts for couples ready to understand
          recurring patterns and rebuild connection with intention.
        </p>
      </div>
      <a class="btn btn--secondary" href="#contact">Ask about the book</a>
    </article>
  </div>
</section>

<style>
  .book-feature__card {
    padding: clamp(1.5rem, 4vw, 2.4rem);
    display: grid;
    grid-template-columns: minmax(120px, 160px) 1fr auto;
    gap: 1.35rem;
    align-items: center;
    background:
      linear-gradient(90deg, rgba(124, 99, 81, 0.08), rgba(124, 99, 81, 0.01) 26%),
      color-mix(in srgb, #f1e5d7 86%, #fff 14%);
  }

  .book-feature__image-frame {
    margin: 0;
    border-radius: 18px;
    overflow: clip;
    border: 1px solid color-mix(in srgb, var(--color-border) 66%, #fff 34%);
    box-shadow: 0 10px 22px rgba(67, 45, 29, 0.12);
  }

  .book-feature__image {
    width: 100%;
    height: clamp(152px, 20vw, 204px);
    object-fit: cover;
    object-position: center;
    filter: saturate(0.9) contrast(0.94);
  }

  .book-feature__card .section__lead {
    margin-top: 0;
  }

  @media (max-width: 820px) {
    .book-feature__card {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }
</style>
