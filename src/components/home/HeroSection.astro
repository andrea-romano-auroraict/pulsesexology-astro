<section class="hero section">
  <div class="container hero__inner">
    <p class="kicker">Counselling & Sexology Support</p>
    <h1 class="hero__title">A gentle place to work through intimacy, connection, and change</h1>
    <p class="hero__lead">
      Whether you are coming as a couple or on your own, this is a grounded space to explore relationship challenges,
      menopause and midlife changes, and your sexual wellbeing with care and respect.
    </p>
    <div class="hero__actions">
      <a class="btn btn--primary" href="#pathways">Explore support pathways</a>
      <a class="btn btn--secondary" href="#about">Meet Simone</a>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: clamp(4.5rem, 10vw, 7rem);
  }

  .hero__inner {
    max-width: 760px;
  }

  .hero__title {
    margin: 0;
    font-family: "Georgia", "Times New Roman", serif;
    font-size: clamp(2.2rem, 6.2vw, 4rem);
    line-height: 1.1;
    font-weight: 500;
  }

  .hero__lead {
    margin: 1.4rem 0 0;
    font-size: 1.08rem;
    color: var(--color-text-muted);
    max-width: 65ch;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 2rem;
  }
</style>
