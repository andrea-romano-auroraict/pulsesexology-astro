<section class="section final-cta">
  <div class="container final-cta__inner">
    <p class="kicker">Begin gently</p>
    <h2 class="section__heading">You are welcome to take the next step in your own time</h2>
    <p class="section__lead">
      If you are ready for thoughtful support around intimacy, relationship change, or sexual wellbeing, Simone is
      here to walk alongside you.
    </p>
    <a class="btn btn--primary" href="#contact">Book a Session</a>
  </div>
</section>

<style>
  .final-cta {
    padding-bottom: clamp(4rem, 7vw, 6.5rem);
  }

  .final-cta__inner {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-soft);
    padding: clamp(1.6rem, 4vw, 2.8rem);
    max-width: 820px;
  }

  .final-cta__inner .btn {
    margin-top: 1.5rem;
  }
</style>
