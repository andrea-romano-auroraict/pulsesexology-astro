<header class="site-header">
  <div class="container site-header__inner">
    <a class="site-logo" href="/">Pulse Sexology</a>

    <nav aria-label="Primary">
      <ul class="site-nav">
        <li><a href="/">Home</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#pathways">Work With Me</a></li>
        <li><a href="#book">Relationships Rewire</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>

    <a class="btn btn--primary site-header__cta" href="#contact">Book a Session</a>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 10;
    backdrop-filter: blur(10px);
    background: color-mix(in srgb, var(--color-bg) 86%, white 14%);
    border-bottom: 1px solid var(--color-border);
  }

  .site-header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    min-height: 78px;
  }

  .site-logo {
    font-family: "Georgia", "Times New Roman", serif;
    font-size: 1.15rem;
    letter-spacing: 0.02em;
  }

  .site-nav {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    gap: 1.3rem;
    color: var(--color-text-muted);
    font-size: 0.95rem;
  }

  .site-nav a:hover {
    color: var(--color-text);
  }

  .site-header__cta {
    white-space: nowrap;
    font-size: 0.9rem;
  }

  @media (max-width: 980px) {
    .site-header {
      position: static;
    }

    .site-header__inner {
      flex-wrap: wrap;
      justify-content: center;
      padding: 1rem 0;
      gap: 0.8rem 1rem;
    }

    .site-nav {
      flex-wrap: wrap;
      justify-content: center;
    }
  }
</style>
